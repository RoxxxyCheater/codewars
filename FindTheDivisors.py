# 7 kyu
# Find the divisors!
# 77915389% of 8,44731,592 of 80,407kwy951 Issue Reported
#  Python
# 3.11
# VIM
# EMACS
# Instructions
# Output
# Create a function named divisors/Divisors that takes an integer n > 1 and returns an array with all of the integer's divisors(except for 1 and the number itself), from smallest to largest. If the number is prime return the string '(integer) is prime' (null in C#, empty table in COBOL) (use Either String a in Haskell and Result<Vec<u32>, String> in Rust).

# Example:
# divisors(12); #should return [2,3,4,6]
# divisors(25); #should return [5]
# divisors(13); #should return "13 is prime"



def divisors(n):
  return res


    def basic_test_cases():
        return True if divisors(15) == [3,5])      
        return True if divisors(253) == [11,23]
        return True if divisors(24) == [2,3,4,6,8,12]
        return True if divisors(25) == [5]
        return True if divisors(13) == "13 is prime"
        return True if divisors(3) == "3 is prime"
        return True if divisors(29) == "29 is prime"

basic_test_cases()
